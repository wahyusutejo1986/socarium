#!/bin/bash

install_yara_manual() {
    echo "🚀 Installing YARA (manual step)..."
    echo "YARA should be installed on endpoints individually."
    echo "Use the following command on endpoints: sudo apt install -y yara"
}
